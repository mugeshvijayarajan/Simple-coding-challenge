#include <iostream>

using namespace std;

int main()
{
    char a;
    cin>>a;
    if(isalpha(a))
    {
        cout<<"is alpha";
    }
    else{
        cout<<"is not a alpha";
    }
    return 0;
}
